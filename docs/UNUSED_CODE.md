# Unused Code Report

The repository contains significant cruft resulting from exploratory analysis and data preprocessing.

### Unused Files & Dead Code

| File / Folder | Why it is unused | Can it be deleted? | What depends on it? |
| :--- | :--- | :---: | :--- |
| `oversample_rider.py` (root) | Replaced by `src/oversample_rider.py`. Hardcodes paths. | **YES** | Nothing |
| `analyze_bdd.py` (root) | Replaced by a module in `src/`. | **YES** | Nothing |
| `clean_balance_bdd.py` (root)| Superceded by `src/` modules. | **YES** | Nothing |
| `count_rider_images.py` | Exploratory script. Not needed for pipeline. | **YES** | Nothing |
| `downsample_bdd.py` | Obsolete logic. | **YES** | Nothing |
| `fix_labels.py` | Temporary hack to fix off-by-one label indices. Fixed natively in `merge_idd.py`. | **YES** | Nothing |
| `remove_train_class.py` | Functionality moved into class mapping dictionaries. | **YES** | Nothing |
| `scripts/train_yolov5s.py` | This is dead code because `scripts/train_orchestrator.py` now wraps the entire execution process natively calling `frameworks/yolov5_src/train.py` | **YES** | Nothing |
| `configs/` (folder) | Directory is empty. Ultralytics relies on YAMLs in `data1/` instead. | **YES** | Nothing |
| `data1/bdd100k_seg` | Segmentation dataset masks. The paper/repository is strictly bounding box detection. | **YES** (saves massive disk space) | Nothing |
| `experiments/yolov5/validation_yolov5s` | Temporary 1-epoch run folder generated by orchestrator. | **NO** | Orchestrator uses it to check baseline setup success. |
